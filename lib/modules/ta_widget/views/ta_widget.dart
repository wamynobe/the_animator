import 'package:flutter/material.dart';

class TheAnimatorWidgets extends StatelessWidget {
  const TheAnimatorWidgets({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('The Animator Widgets'),
    );
  }
}
